# プロジェクトのコーディングルール

このプロジェクトにおける各種ファイルの実装ルールです。実装を進める際は必ずこのルールを参照し、遵守してください。

## 1. CSS (SCSS)

CSS は `scss/` フォルダ配下に作成し、以下の構成で分類してください。

- **component**: 単品の要素（Atoms のようなイメージ）
- **foundation**: 共通の項目（リセット、変数、ベーススタイルなど）
- **ui**: 複数の要素を組み合わせたコンポーネント（Molecules のようなイメージ）
- **pages**: ページ特有の実装を追加していくファイル`
- マージンやフォントサイズなどは 1rem = 10px 相当として rem で指定してください
- 二箇所以上で使われている色は foundation/colors.scss に切り出して定義してください

## 2. JavaScript

JS は `main.js` へ実装してください。

- **共通の JS**: 共通のハンバーガーメニューのような全ページで動作する JS は、そのまま実装します。
- **ページ固有の JS**: `if ($.Top) {}` のような形で、ページ毎に固有のクラス等を付けてスコープし、該当ページでのみ動作するよう実装してください。

## 3. 画像ファイル

- 画像は Figma からダウンロードし、 `public/img` に配置してください。
- Figma で **SVG** が指定されているものは、そのまま SVG 形式でダウンロードして使用してください。

## 4. html ファイル

- html のコンポーネントは `src/components/` に配置してください。
- ページ毎の実装は index.html などに行います

## 5. Figma からの実装手順

Figma MCP を使ってデザインを実装する際は、以下の手順を必ず守ること。

1. **`get_screenshot`** でビジュアル全体を把握する
2. **`get_metadata`** で各ノードの **正確な px 寸法（width / height / x / y）** を取得する（★最重要）
3. `get_design_context` は構造の参考としてのみ使用する（Tailwind のパーセンテージ値をそのまま使わない）
4. 取得した px 値を `vwpc()` / `vwsp()` に変換して SCSS に適用する
5. **画像・SVG には必ず width と height の両方を指定する**（`height: auto` にしない）
6. 画像アセットは `curl` で `public/img/` にダウンロードし、SVG 指定のものは SVG 形式のまま保存する

> **注意**: `get_design_context` が返す React+Tailwind コードはあくまで構造の参考情報。
> 寸法・配置は必ず `get_metadata` の値を使うこと。
